import { Progress } from "@/components/ui/progress";

export const progressProps = [
  {
    name: "value",
    type: "number",
    default: "0",
    description: "進捗率（0〜100）",
  },
];

# Progress

<Description>タスクの進捗状況を視覚的に表示するプログレスバーコンポーネント。</Description>

## Usage

<ComponentPreview>
  <Progress value={60} className="max-w-sm" />
</ComponentPreview>

<CodeBlock code={`import { Progress } from "@/components/ui/progress";

<Progress value={60} />`} />

## Examples

<ComponentPreview className="flex-col gap-4">
  <Progress value={0} className="max-w-sm" />
  <Progress value={30} className="max-w-sm" />
  <Progress value={60} className="max-w-sm" />
  <Progress value={100} className="max-w-sm" />
</ComponentPreview>

<CodeBlock
  code={`<Progress value={0} />
<Progress value={30} />
<Progress value={60} />
<Progress value={100} />`}
/>

## Props

<PropsTable props={progressProps} />
